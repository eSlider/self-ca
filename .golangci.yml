run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - bodyclose

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - bodyclose
        - errcheck
